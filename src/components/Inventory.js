function Inventory() {
  return (
    <div>
      {/* Inventory header */}
      {/* navbar/buttons to about and contact */}
      {/* sidebar with logo & filter */}
      {/* main div with grid of beers. could be similar to image gallery i made of cats. clicking beer opens a modal with its information? */}
    </div>
  );
}

export default Inventory;
